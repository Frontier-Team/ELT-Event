export interface Event {
  id: number;
  time: string;
  topic: string;
  speakers: string[];
}

export interface Portfolio {
  name: string;
  occupation: string;
  details: string;
  contact: string;
}
